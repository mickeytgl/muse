<% @posts.each do |post| %>
<h2><%= link_to post.title, post %></h2>
<%= link_to (image_tag post.image.url(:small)) %>
<p><%= pluralize(post.comments.count, "Comment") %></p>
<% end %>

<p><%= link_to "Add new Inspiration", new_post_path %></p>